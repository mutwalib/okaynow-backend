package com.okaynow.booking.domain;

public enum ClaimSource {
    /** Caregiver self-claimed an OPEN marketplace shift. */
    MARKETPLACE,
    /** Agency recruited / assigned the caregiver to the shift. */
    ASSIGNED
}
