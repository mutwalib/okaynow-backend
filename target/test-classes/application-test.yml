spring:
  datasource:
    url: jdbc:h2:mem:okaynow-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-secret-key-for-junit-only-0123456789abcdefghijklmnop
    access-token-validity-seconds: 900
    refresh-token-validity-seconds: 1209600
  cors:
    allowed-origins: http://localhost:3000
  bootstrap-admin:
    email: admin@okaynow.com
    password: admin123
